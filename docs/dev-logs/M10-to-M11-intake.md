# M10 -> M11 Intake

裁决：**有条件通过**。

## 合并状态

- M10 功能提交 `02cd8d4` 已进入 `origin/main`，合并提交为 `8531740`。
- M11 从 `origin/main` 新建 `codex/m11-quality-regression`，没有继续叠在旧功能分支上。

## 验收抽查

- `npm run typecheck --prefix app` 通过。
- `npm run test --prefix app` 通过：21 个测试文件、97 项测试，行覆盖率 88.22%。
- 候选记忆测试覆盖闲聊过滤、可追溯摘录、即时写入绕过候选与精确去重。
- 调研与雷达测试覆盖体检、搜索/打开失败、成功来源元数据、任务审计与补跑计划。
- `git diff --check origin/main...02cd8d4` 无差异或空白错误；M10 提交未带入测试产物、用户数据库或私钥文件。

## 条件与后续

- 真实 `agent-reach doctor` 已通过，但 M10 现场调用 Exa 时外部连接超时；失败路径已证明会留下任务审计且不写假来源。
- 外部依赖恢复后仍应补一次完整真实搜索成功链的现场证据。该项不改变 M10 的账本纪律，也不阻塞 M11 质量回归。
- M11 金标与任何真实端点试跑必须继续使用临时数据库，测试数据不得进入用户大脑文件。
