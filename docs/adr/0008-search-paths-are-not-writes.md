# 检索路数与入库条数无关；搜不到不写、不编、不写负事实

把「搜了几路」和「必须写入几条」绑在一起，会逼系统用模型常识凑来源。空检索再强制生成会升高幻觉（sufficient context：上下文不足时幻觉可从约一成跳到六成以上）。StaffDesk：检索可以多平台多路；只有成功打开、有原文可指的材料才入库。抽不出可核对命题就不产生主张。搜不到不能写成「世界上没有」；「没找到招股书」不是主张「该公司没有招股书」。查询词、平台、失败 URL、停止原因记入任务审计（回放），不是来源，不能当主张出处。编造的引用必须丢掉。失败表现为未知。不准为凑条数写入假来源或假主张。
