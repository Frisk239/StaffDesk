# 第五轮审计（M35 审计刀，2026-09-02）

基准树 main@a912f5d（M34 合并树）+ F1 热修 cherry-pick（474f1c7）。三路子代理（设计符合性 / 技术债 / 功能缺口）+ Owner 全量门禁；审计刀不改产品代码，修复进后续刀施工单。Owner 已定点复核载重结论（P2-1 排序键失效与 G1 README 缺失亲验坐实）。

## 一、总评

巩固期三刀（M32-M34）账目全部验真：上轮 D1-D6 还清无虚报，上轮 P-A/P-B/P-C 修复全部落地，信号面满分（any/ts-ignore/TODO/console.log 全零，覆盖 92.82%），两类已修 CI flake 零复发。**项目状态：产品内部已到收尾水位，缺口集中在外壳**——README/Release/LICENSE/视觉素材是把 63 份 ADR 与测试金字塔兑换成简历叙事的拼图。另有一件运行事件：F1 安装器终验首跑失败（main 两连红，热修 #31 探测式已开待合）与一个真 bug（P2-1 排序键失效）。

## 二、设计符合性（新发现 P2 ×1 / P3 ×1；上轮修复全验证通过）

| # | 级别 | 发现 | 现状证据 |
|---|---|---|---|
| P2-1 | P2（真 bug） | **ADR 0063 排序键运行时失效**：persist.ts:801 `SELECT id, action, created_at` 以 `as OperationRow[]` 强转，接口字段是驼峰 `createdAt`——运行时恒 undefined，排序退化为纯 id 字典序。生产近似等价（id 前缀含 Date.now()）但备份恢复/时钟回拨下偏离承诺键；测试有盲区（纯函数用例全驼峰构造、集成断言两种排序下均过） | operationsRetention.ts:29/45 vs persist.ts:801；Owner 亲验 |
| P3-1 | P3 | 「简报」词条滞后：M32 后实现有主键来源标注与出站导出，词条正文未跟进 | CONTEXT.md:172 vs BriefView.tsx:81-105 |

干净线：Avoid 全线零违规（内部机制名 0 命中、脚注/日志导出文案合规）；记忆三词条与 Settings 节逐字对齐；硬顶/快搜/检索/多路与词条逐字一致；0058/0062 无回归、0063 豁免拼写逐字一致且 append 后裁终态等价裁定复核成立。**上轮修复验证：P-A 跳数已删、P-B 简报主键标注已兑现、P-C 检索措辞已改——全部通过。**

## 三、技术债

旧账：D1（fetch 超时）/D2（拆分）/D3+F5（renderer 测试）/D4（0063 实现）/D6（时长记录）**全部验真已还**；D5 观察项无恶化。M34 遗留：fromUrl 死分支仍在（sourceActions.ts:375/387）、SET_VIEW 自赋值仍在（objectChatActions.ts:52）、EXTRACT_DONE 疑点销案（误报更正确认）、enqueueWrite 四守卫重复仍在、tests/perf 镜像偏差记档接受。

新债：

- **E1（高危）main 常绿已破**：M33/M34 两次合并的 package job 连红（`installed exe not found`——落点假设错坐实）；热修 #31（探测式三根扫描+诊断列目录）已开待合，**合并后盯首跑**。
- **E2（中）**：两处死噪音顺刀清（fromUrl 死分支 + SET_VIEW 自赋值）。
- **E3（中低）**：enqueueWrite 四守卫块合并为前置校验（约 -24 行）。
- **E4（低，观察）**：operationsRetention 稳态放大器（超限后每 dispatch COUNT 全扫；单人年量级毫秒级）。
- **E5（低，记档）**：renderer stub 语义边界（dispatch 只复刻 TOAST、chatSend 同步返回）。
- **E6（低）**：Settings.tsx 1489 行成新 top1（persist.ts 1344 次之）——下轮拆分候选。

信号面：any/@ts-ignore/@ts-expect-error/TODO/console.log(src)/eslint-disable = 0/0/0/0/0/1（存量）；文件 top5：Settings 1489 / persist 1344 / Chrome 731 / objectChatActions 668 / runner 640。CI 健康：超时预算充足、F6 阈值放宽 50-500 倍安全、F3 日志全冷路径、F2 定时器已 unref。

## 四、功能缺口（收尾期视角，G1-G7）

- **G1（最高）README 全仓缺失**：GitHub description/topics 全空，面试官无从下手——简历链接的第一落地页，缺它其余亮点全部不可见。
- **G2 安装包无持久渠道 + tag 断档**：artifact 7 天消失，tag 停在 v0.1.0-m1-m7（27 刀无 tag）——收尾打 v0.2.0 + package job 追加 Release 上传（约 10 行 yml）。
- **G3 LICENSE 缺失**：MIT 一个文件。
- **G4 零视觉素材 + ADR 无索引**：3-5 张关键截图进 docs/assets + ADR 按主题索引页——面试前三分钟的杠杆。
- **G5 质量数字未沉淀对外**：README 质量节 + CI badge + 补 dependabot（M33 记账未落地）。
- **G6 渲染层广播无护栏**：store.tsx context value 未 memo、全量 setState 全树重渲染；F6 只护 reducer/持久层——先 memo + 渲染计时阈值，超线再谈 selector。
- **G7 onboarding 后引导浅**：Inbox 空态补两步引导文案。

查过且干净：onboarding 全链/托盘三态/错误恢复三入口（旁置+诊断导出+备份）/gitignore/数字核验（ADR 63 份、e2e 38）。

## 五、Owner 门禁（基准树 a912f5d+热修，数字机械摘取自命令输出）

build / typecheck / lint / native:check 通过；test **51 文件 / 359 测试**，Lines **92.82%**（门槛 ≥80%）；eval 四阶段通过；e2e **38 通过（22.0s）**。

## 六、M36+ 排期（建议）

- **M36 收尾面刀**：G1 README（中文：定位/快速跑起来/架构图/质量数字/精选 10 份 ADR）+ G2 Release 链路（tag v0.2.0 + 上传步骤）+ G3 LICENSE + G5 badge/dependabot；**顺刀修复**：P2-1（SQL 别名一行 + 测试盲区补）、E2 两处死噪音、P3-1 简报词条补。
- **M37 体验与叙事刀**：G4 截图与 ADR 索引 + G6 渲染层护栏 + G7 引导文案 + E3 守卫合并。
- 观察：E4/E5/D5。
- 下次审计刀 **~M40**（M35 数起第五刀）。
- 事件记录：F1 终验首跑失败（假设未经真机验证）→ 热修 #31 探测式 + 本档记档；真实落点待 #31 合并后 main 首跑日志钉死。
