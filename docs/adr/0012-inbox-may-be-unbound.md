# Inbox 允许未绑定；上对象页必须绑定且不自动乱绑

强迫每次丢材料先选对象，捕获摩擦大，来源会留在系统外。GTD、PARA、Readwise、Heptabase Inbox、GBrain 的 inbox/ 捕获都是先收再归。自动把来源派到错误对象，比晚归更糟。StaffDesk：Inbox 可暂存未绑定来源，能打开原文、能搜到。未绑定不抽进对象投影，不进对象对话默认语境。绑定后才抽取并上页；一份来源可挂多个对象。Agent 可建议绑定对象，须人确认。Inbox 是可见的待处理列表，不是黑洞。
