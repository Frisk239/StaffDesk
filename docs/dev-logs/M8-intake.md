# M8 审计接收

日期：2026-08-30

## 裁决

**有条件通过**

M0–M7 主链可运行；M8 的显式抽取终态、真实全局模型配置与真实端点抽取已经在本地工作区验证，但 M8 变更尚未形成提交，且真实进料前仍有会放大主链风险的缺口。

## 合并与证据状态

- `origin/main` 与审计时 HEAD 均停在 `7dfde30`，M8 仅存在于未提交工作区。
- 审计复跑 `lint`、`typecheck`、60 项测试与 Electron E2E 均通过。
- 隔离临时大脑复用产品全局配置 `opencode-zen / deepseek-v4-flash`，成功抽出 4 条未核主张；临时样本未写入用户业务库。

## 本刀必须收口

1. 重抽主张 ID 不得撞主键，并保持重试幂等语义。
2. 一次丢弃多条未核主张也必须产生可撤销的补偿动作。
3. 来源提供独立解绑回 Inbox 与删除入口；删除按账本规则级联关窗并展示影响数。
4. 抽取 IPC 与渲染层调度使用失败终态和 `finally` 释放占位，不得永久停在“抽取中”。
5. 窄窗首次打开也能看到或主动打开右栏，不得依赖首次 resize 才修正布局。
6. 上述收口与当前 M8 变更一起形成独立分支提交，经全链路回归后再放行 M9。

## 延后但记录

- 调研不可用终态、向导“继续设置”、批量晋升队列持久化、绑定结果卡终态文案、只读工具测试、流式工具增量合并和抽取映射质量进入后续债务池。
- URL/PDF 获取、长材料分块仍属于 M9，不混入本刀。
