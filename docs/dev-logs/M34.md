# M34 — structure and guardrails: split the reducer, cap operations, test the renderer

## Scope

审计四轮排期收尾刀（巩固期最后一刀）：D2 拆 applyAction（3293 行 / 80 case，纯搬运）、0063 operations 裁剪实现（ADR 已落档）、F5/D3 renderer 行为测试首批 + chat 失败 e2e、F6 规模基线进 CI、D6 e2e 时长恢复记录。两批执行者串行（第一批 0063+护栏，第二批 D2——地盘不相交，第二批只碰 applyAction.ts 与新域文件）。

## Changes

### 第一批（0063 + 护栏）

- **0063 operations 裁剪**：`operationsRetention.ts` 纯函数（LIMIT=20_000 单人年量级、豁免 {DELETE_SOURCE, CORRECT_CLAIM, SET_SOURCE_ROLE} 永不裁、不变量注释在案）+ persist `pruneOperations`（COUNT 探底→超限才 SELECT 轻量行→事务内批量 DELETE）+ index.ts appendOperation 后接线。**已核实记档**：恢复查询 M28 已走 `WHERE action=` + v7 索引，不重复施工。**时序附注（评审澄清）**：ADR 原文「先裁再写」对齐 taskAudits 内存态批量模型；operations 逐条直插 SQL 无内存态，且「先裁再写」与「恰好等于上限不动手」裁决在逐条模型下互斥——实现为 append 后 prune，两步同步执行无观察者、新行必不入被裁集，终态与全部不变量等价（防 M36 审计误判，特此记档）。
- **F5/D3 renderer 测试层从零到一**：devDeps + happy-dom + @testing-library/react；vitest include 扩 `{ts,tsx}`、tsconfig.web/eslint 同步；组件测试按文件 `@vitest-environment happy-dom` 切换（全局 node 与串行不动）。首批三条：记忆节（分区/REMOVE_MEMORY/0054 三元组/空态）、chat 失败态（TOAST 出现→3.2s 消散、用户消息保留、busy 禁发与恢复、失败后再发）、**谓词表交互（Spec 评审补：编辑弹层保存走 UPDATE_SLOT、删除确认走 REMOVE_SLOT、取消不派发）**——mock 边界一律 window.staffdesk IPC 桩。
- **chat 失败 e2e**：installStubModel 后 closeServer 注入连接失败（重试 3 次约 1s），断言失败 TOAST/消息保留/busy 解除可再发；零产品改动。
- **F6 规模基线**：tests/perf/scale-baseline.test.ts 两层（5k claims 纯 reducer 内存层 + 临时库全链路层），阈值=本机实测数量级放宽（轻 2ms→1000ms、重 1.3ms→2000ms、全链路 98ms→5000ms），注释标「数量级护栏非精确度量」。

### 第二批（D2 拆分）

- **applyAction.ts 3293 → 86 行壳**：8 条账本规则注释、`export type { Action }`、`reducer`、`export { reducer as applyAction }`、6 个零使用者 projection 导出全部原样保留。分发 = 7 域处理器 `??` 链 + 兜底 state；域处理器返回 undefined 表示不归本域。
- **8 个新文件**：actionHelpers.ts（196 行跨域共享 + ReducerFn 类型）、sourceActions（494）、claimActions（329）、writeQueueActions（375，唯一收 reducer 参数做递归 dispatch——CONFIRM_WRITE 五处 + UNDO_RESULT.设角色一处回调壳入口）、proposalActions（567，PROPOSAL_DECIDE 460 行独占）、taskActions（216）、slotTemplateActions（507，applyScenarioTemplateUpsert 供 writeQueue 跨域单向 import）、objectChatActions（668）。域文件零 import 壳；actionHelpers 零 import 域文件。80 case 机械对账集合一致（3+38+5+5+11+5+13）。
- **纯度认证（Standards 评审全量机械 diff）**：66 case 块与 39 helper 逐字一致；唯二非行为差异=分发结构必需的 return undefined + prettier 参数换行。

## Verification（数字机械摘取自命令输出）

- build / typecheck / lint / native:check：通过
- test：51 个测试文件 / 359 个测试通过（评审整改 +2：谓词表交互）；覆盖率 Lines 92.76%（门槛 ≥80%）
- eval：四阶段通过
- e2e：38 个通过
- **D6 e2e 时长趋势（恢复记录）**：M27 21→M28 25→M30 30→M32 34→M33 37→**M34 38（18.6s，本刀全量）**；趋势随规格数线性，无异常跳变。
- **等价护栏**：D2 前后 50 文件 / 357 测试同数字全绿（评审补谓词表交互后 51/359）。

## Code review（/code-review 两路子代理，基点 main）

**Standards**：零硬违规——纯搬运经全量机械 diff 认证；import 方向合规（域不反向 import 壳、helpers 不 import 域、writeQueue→slotTemplate 单向）；devDeps 只进 devDependencies；tsx mock 零外网。判断题三笔：enqueueWrite 守卫重复（基点固有，留后续刀）、tests/perf 打破目录镜像约定（记档接受——按测试类别分目录与 tests/eval 同理）、claimActions 缺域头注释（**已补**）+ 易腐行号注释（**已改**）。
**Spec**：特别核对全过（豁免拼写、D2 壳导出、恢复查询索引、mock 边界、F6 阈值依据）；**真缺口一笔已补**（谓词表交互测试）；0063 时序偏差裁定合理（附记如上）；一批疑点③（EXTRACT_DONE 可选链）经核系误报——`action.outcome ?? 'success'` 处理联合可选分支非冗余，记档更正定性。

## Deviations / Notes

- 执行者自报偏差均核实合理：接线测试用 INSERT 灌 20_050 行验证（不逐条 dispatch）；「槽编辑族 9 个共用」修正为 2 个跨域入 helpers；objectChatActions 668 行未再拆（<800 阈值）。
- **行为疑点清单（D2 红线只报未修，留后续刀或审计）**：① ADD_SOURCE 标题回退的 fromUrl 死分支；② SET_VIEW selectedClaimId 自赋值；③ EXTRACT_DONE 冗余可选链（Spec 评审核为误报，非冗余）。
- tests/perf/ 目录镜像偏差记档接受。

## Debt carried / next

- enqueueWrite 四守卫块重复（基点固有）留顺手队列。
- **M35 = 空刀位（巩固期三刀已清，审计刀 ~M36 到期）**——下刀即审计刀（第五轮，三路子代理 + Owner 门禁），或按 Owner 判断提前。
