# M4 调研任务

状态：自测完成。可与 M2/M3 一并验收。

## 做了什么

- 适配层：体检 `agent-reach doctor --json`，搜索 `mcporter call exa.web_search_exa`，打开 `r.jina.ai`。本机没有工具时返回安装引导，不崩。
- 任务引擎：快搜/深挖硬顶（搜索次数、打开次数、步数、墙钟）。触顶后已打开的来源照写入库（调研路径、未核抽取），失败 URL 进任务审计。不编负事实。
- 对象顶栏「调研」开快搜；「回放」看步骤。

## 怎么点

打开对象 → 调研。若本机没装 Agent Reach，会提示安装，不会写假来源。装好后会检索、打开、入库、抽取。回放能看到每步和失败 URL。

## 测试

注入假检索：成功打开的入库、失败 URL 在审计、来源 path=调研、正文不含「没有这家公司」。适配层 doctor 失败带安装引导。
