# 回放挂任务；单人一份大脑；召回不展览后端

LangSmith 把一次 run 做成可看的轨迹，不是把聊天当审计。GBrain 从个人大脑起家。对本机已有材料的召回，业内用关键词加向量，关系用于跳转，不必把论文名做成功能。StaffDesk：回放挂在任务上，能停、能看步骤、能看到搜过与打开过什么。单人、界面中文、OpenAI 兼容接口、不做多租户。来源先支持文本、URL、PDF。召回承诺找全找对；默认关键词加向量，关系用于「谁在哪」。不上 LightRAG、KAG、Graphiti 当功能名。评测看 Recall、Precision、MRR、忠实。对外搜仍叫检索，与召回不是同一件事。
