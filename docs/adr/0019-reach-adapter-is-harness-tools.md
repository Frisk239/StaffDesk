# 适配层是 harness 的工具，不重写 Agent Reach

Agent Reach 是安装、体检和上游路由，真正搜和打开的是各平台 CLI。它的 Skill 是给 coding agent 拼命令用的，不能当参谋台运行时。StaffDesk 用 TypeScript harness 循环；适配层把 doctor、搜索、读 URL 收成模型可调、结构稳定的工具，计入任务审计和硬顶。成功打开的才入库。适配层不抽主张、不绑定、不晋升。先接网页、全网搜索、GitHub，其余按任务再加。Python 只作为本机 Agent Reach 依赖。
