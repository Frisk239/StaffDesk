# 第四轮审计（M31 审计刀，2026-09-02）

基准树 main@58a796d（M30 合并树 = 功能面齐平达成后的第一棵树）。三路子代理（设计符合性 / 技术债 / 功能缺口）+ Owner 全量门禁；审计刀不改产品代码，全部修复进后续刀施工单。Owner 已定点复核载重结论（快搜词条跳数、brief 零 role 引用、reach 两处 fetch 无 AbortSignal、崩溃防线零命中、BriefView 零导出——逐条验真）。

## 一、总评

功能面齐平达成后项目状态健康：上轮债还得干净无虚报（M28 四笔全部文件级验真），信号面持续满分（any/ts-ignore/TODO/console.log 全零，覆盖 92.7%），近五份 ADR（0058-0062）除一笔外全部落位。**风险集中在新面**：reach 网络路径无超时（任务可挂死，D1）与巨型 reducer（applyAction.ts 3289 行，连续三轮审计 P1 全落此文件，D2）。巩固期主题定为：**装得上、起得来、坏了能救、产出能带走**（安装器终验、崩溃恢复、日志可观测、简报导出）。

## 二、设计符合性（P1 零 / P2 两笔 / P3 一笔）

| # | 级别 | 发现 | 现状证据 |
|---|---|---|---|
| P-A | P2 | 「快搜」词条仍列「跳数」硬顶——与 0059（hops 删除）、engine Budget、M28 进度条目三重矛盾 | CONTEXT.md:185；engine.ts:8-16 无 hops |
| P-B | P2 | 0062 承诺「简报标注按当前对象视角取角色」未兑现——buildBrief 全文零 role 引用，M29 验收未记为裁剪 | shared/brief.ts（0 命中）；AuditCard.tsx:54 审计卡侧已兑现 |
| P-C | P3 | 「检索」词条「跟踪链接」措辞暗示跳链，0059 已明示引擎无跳链 | CONTEXT.md:93 |

Avoid 列表全线干净（内部机制名 0 命中、「未知」9 处全占位语义、出站闸 briefOut/briefGen 合规）；候选记忆逐轮即产、费用 token 口径、0058-0061 承诺逐条落位（含「端点未回传 usage」逐字、失败路计数测试、GitHub 恒绿注释）。

## 三、技术债现状（对照 M21 基线与上轮清单）

旧账：双 loadLedger / syncTable 全表 / taskAudits 无界 / FTS 死悬 **四笔验真已还**；operations 扫描部分还（已移出 snapshot，表本身无保留策略→D4）；renderer 零行为测试**仍在（tests/renderer/ 两文件实为纯函数+字符串断言，非组件行为测试）**；chat 失败 e2e 仍零（兜底单测已有）。

新债按性价比×风险：

- **D1（高危）**：reach 两处 fetch 无超时（reach.ts:156 GitHub 搜索、:246 Jina open 均无 AbortSignal）+ 墙钟只在步间判（engine.ts:343/373）= 任务可永久挂死；LLM 侧 60s 超时对照。刀法：AbortSignal.timeout 对齐 spawn 25s/20s + e2e 挂起注入断言墙钟收口。
- **D2（高性价比）**：applyAction.ts 3289 行 / 80 case 单 switch。刀法：按域拆 reducer（来源/槽/模板/write_queue），纯搬运不改行为，dispatch-read seam 护栏。
- **D3（中）**：Settings.tsx 1362 / ChatPane.tsx 506 零交互测试；缺「chat 失败 TOAST」e2e。
- **D4（中）**：operations 每 dispatch 一行、payload 含快照、无上限；listDeletedSourceRecoveries 全扫 JSON.parse。**需先 ADR 定 undo 留痕期限**（域难，开刀时盘问）。
- **D5（低）**：GitHub 403/429 无退避（天然软上限，观察）；failedPaths/pathOutcomes 重叠（M30 评审已记录接受）。
- **D6（低）**：runCommand stdout 无界累积；e2e 时长 M28 起断记（趋势不可观测）。

信号面：any 0 / @ts-ignore 0 / TODO 0 / console.log 0（warn 5 处合规）/ eslint-disable 1 处存量；文件 top：applyAction 3289 / Settings 1362 / persist 1323 / Chrome 731。

## 四、功能缺口与巩固期方向（F1-F8，按价值排序）

- **F1** NSIS 安装器从未被「安装+启动」验证（CI package job 只打包上传；M7 后无人再装）——交付链路从「能打包」变「装得上、起得来」。
- **F2** 崩溃/断电恢复零防线：无 crashReporter/uncaughtException/render-process-gone；损坏库直接抛无备份恢复引导；无「中途杀进程→重开一致」测试——「数据不丢」故事与账本纪律呼应，成本低。
- **F3** 零持久化日志（仅 5 处 console.warn）；redact 掩码纪律现成无沉淀面——可观测性零到一，掩码纪律顺势成卖点。
- **F4** 简报无出站出口（BriefView 无复制/导出）——参谋台核心产出首次能离开应用。
- **F5** renderer 行为测试层薄（组件 5750 行 vs 两个测试文件）——与 D3 合并处理。
- **F6** 全量快照广播无规模护栏（每次 dispatch 全量推 State）——先加 5k claims 规模基线测试进 CI 再谈增量化。
- **F7** 记忆管理界面弱兑现（state.memories 唯一渲染点在待确认页角落；三个记忆词条无管理面；REMOVE_MEMORY 底座已有）。
- **F8** unparsed 旧材料无重导入口（CONTEXT 明示应重导，现有 URL 可走既有 ingestUrl）。

查过且干净：工作区全生命周期、简报多份历史、材料删除级联+恢复+解绑、Onboarding 五步完整、备份/恢复含安全备份、任务审计保留与 FTS 触发器。

## 五、Owner 门禁（基准树 58a796d，数字机械摘取自命令输出）

build / typecheck / lint / native:check 通过；test **43 文件 / 308 测试**，Lines **92.7%**（门槛 ≥80%）；eval 四阶段通过；e2e **30 通过**。

## 六、M31+ 巩固期排期（建议）

- **M32 加固收尾刀**（小活高价值头班车）：P-A/P-C 词条修订（快搜删跳数、检索剪「跟踪链接」）+ P-B 简报主键标注补兑现（buildBrief 按当前对象视角给冲突/主张标主键来源）+ D1 fetch 超时 + F4 简报复制/导出 + F8 unparsed 重导。
- **M33 恢复与可观测刀**：F2 损坏库恢复引导 + 中断一致性测试 + main 兜底 handler；F3 日志滚动落盘 + 设置页导出诊断日志（复用 redact）；F7 Settings 记忆节；F1 安装器终验（CI 静默装 `/S` + 启动断言，或用户真机终验记录在档）。
- **M34 结构与护栏刀**：D2 applyAction 按域拆分（纯搬运）+ F5/D3 renderer 行为测试与 chat 失败 e2e + F6 规模基线进 CI（顺手 dependabot）+ D6 e2e 时长恢复记录。
- **ADR 队列**：D4 operations 保留策略（undo 留痕期限——域难，M32/M33 开刀时盘问产出）。
- 下次审计刀 **~M36**（M31 数起第五刀）。
