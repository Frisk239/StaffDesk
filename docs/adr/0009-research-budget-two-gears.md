# 调研硬顶管停搜，分快搜与深挖两档

无限搜会烧费用并灌入未核材料；按「必须写满 N 条」设顶又会逼系统编造。业内生产 Agent 同时卡步数、工具调用、墙钟和费用；Deep Research 分 Fast/Deep；GBrain enrichment 按对象分档花钱。触顶应保留已获取材料并记录停止原因。StaffDesk：调研任务默认快搜，深挖须显式打开。快搜默认：搜索 8、打开 12、跳数 1、步数 16、墙钟 3 分钟、费用约 $0.3。深挖：搜索 20、打开 30、跳数 2、步数 40、墙钟 15 分钟、费用约 $1.5。同一查询+平台最多重复 2 次，单 URL 失败重试 3 次。触顶：已打开的入库（未核），其余未知，审计记下闸和搜过什么。不编、不写负事实、不作废半成品。费用数字可按国内 API 再标定；未标定前以步数、打开次数、墙钟为准。
