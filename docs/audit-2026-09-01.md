# StaffDesk 第三轮审计报告（2026-09-01，审计刀）

> 新版 slice-owner 审计刀首跑（计数触发：M22 起第 6 刀；补课触发：M25/M26 未跑 code-review）。四路并行：设计符合性 / 技术债 / 功能缺口 / 未合增量 fixed-point 评审（c37f847..09b6293）。本刀不改产品代码，发现进后续刀施工单。触发线与范围见 `docs/dev-logs/M26-to-audit-intake.md`。

## 总评

**M25/M26 对 ADR 0057/0058 的落地质量高、无虚报**：设计路线对 0057 十条 / 0058 十四条裁决逐条对账全部落地（reducer/persist/UI/单测/e2e 五层有真实证据），「保护已解除」两侧均成立且有反向断言；评审轴复跑 tsc/eslint/prettier 干净，Standards 轴通过、Spec 轴有条件通过（一项 P1）。领域红线全部守住。但四路 converge 出**两处级联边界的正确性缺陷**（一处在三路独立命中）、一处 UI 真 bug、以及功能面词条重扫发现的**两块此前三轮都漏排的 P1 承诺**（主键标记、向量召回）。

## 一、必须修（进 M27 施工单，前两项可作 #19/#20 合并后小补丁刀）

| # | 级别 | 问题 | 证据 | 修法 |
|---|---|---|---|---|
| F1 | **P1**（设计 V1 + 技术债 + 评审三路 converge） | 删自定义模板不清 `slot_defs.scenarios` 悬挂引用：勾过该场景的槽在 `slotsForScene` 下永不匹配，**从所有对象页静默消失**（主张仍在账本）；与改名级联（清了槽引用）不对称 | `applyAction.ts:1929-1946` 只查 workspaces；`scenario.ts` slotsForScene 过滤 | REMOVE 成功路径级联剔除各槽 scenarios 数组中的该名（槽退化近似通用），或并入拒绝条件并 toast 列数；补 ADR 0058 一句删除级联口径 + 单测 |
| F2 | **P1**（设计 V1） | 挂起的 durable write_queue「整理」行不在 0057 级联面内，且 `CONFIRM_WRITE` 找不到提议的回退分支**无 slotIsControlled 校验**——槽改名/删除把提议撤成 pending:false 后确认必走无守卫分支，把主张写回旧/已删谓词名 | `applyAction.ts:2139-2149`（`predicate: head.targetPredicate!` 裸写）；`withdrawTidyProposalsAbout` :163-180 只扫 state.proposals | 回退分支补受控表校验（toast 拒）；级联时同步撤/改写挂起 write_queue 行的 targetPredicate |
| F3 | **P2 真 bug**（技术债 B-5） | Settings 对话框 Esc 双关：内层槽/模板编辑对话框与外层设置面板同时关闭（两个全局 keydown 无分层） | `Settings.tsx:56-63` vs `:408-414/:533-539/:676-682` | 内层 handler `stopImmediatePropagation` 或层级判定，一行级修复 |
| F4 | **P2**（技术债 A-9 + 评审双命中） | persist-diff 等价护栏没罩住新表：`BUSINESS_TABLES` 缺 `scenario_templates`、剧本缺模板 CRUD 与 UPDATE/REMOVE_SLOT——0056 等价网对 M26 最新的持久化路径失明 | `persist-diff.test.ts:468-492` | 清单加表名 + playScript 追加新动作，半小时级 |
| F5 | **P2** | `tidy-proposals.spec` 启动不带 `--user-data-dir`（与存量 persist/layout 同病）：model-settings/qualification 写进真实用户 profile | `tidy-proposals.spec.ts:40,150` | 补隔离参数，顺手修存量同病三处 |

## 二、词条缺口（功能面齐平的真正剩余清单——两块三轮漏排的 P1）

| # | 词条依据 | 现状 |
|---|---|---|
| G1 | 「主键」词条（CONTEXT:109）**整块未兑现**：由使用者标记/系统可建议/转述不得自动过时/新版过时旧版 | `SourceRole` 类型与展示 tag 存在，但**无任何 action/UI 能设置它**（首轮审计提过一句，从未进里程碑） |
| G2 | 「召回」词条（CONTEXT:93）「默认关键词加向量」 | 只有 trigram FTS，零向量路径；ADR 0042 是暂缓非改判 |
| G3 | 「检索」词条「多平台、多路并行」 | 单路单平台硬编码 |
| G4 | 「快搜」硬顶六维 | 费用缺 + **hops 声明未执法**（hitCap 不查、引擎无跳链行为） |
| G5 | 资格徽章「持续可见」+「首启默认跑」 | 徽章仅 Settings/Onboarding 两处；检查纯手动按钮 |
| G6 | 任务列表打磨 + 失败重跑入口 | 平铺无筛选；失败任务无「再搜一轮」入口 |
| G7 | 关系面板搜索 | picker 无过滤 |
| G8 | 候选记忆「会话结束才产」 | 实现是每轮即抽（更优），词条/ADR 0022 与实现漂移——改词条一行 |
| G9 | 评测集「改后必须复跑」 | 指纹机制通但无版本守护闸（纯自觉）；仿 eval-gate invariant 加一条守护测试 |

## 三、技术债现状（对照 M21 基线）

七笔已知债全部持平（双 loadLedger、syncTable 全表 SELECT、taskAudits 无界、FTS 触发器死悬、renderer 零行为测试、chat 兜底/busy 无 e2e）；operations v7 索引后 listDeletedSourceRecoveries 降为索引查（变好，但 JSON.parse 全量载荷 + 双 snapshot 放大仍在）。全仓信号面持续满分（0 any / 0 ts-ignore / TODO 0 / console.log 0，eslint-disable 1 处存量）。**最值得还的五笔**（按性价比×风险）：① persist-diff 补新表护栏（=F4）；② Settings 对话框行为测试破零 + 修 Esc（=F3）；③ 级联边界两处（=F1/F2）；④ taskAudits 主键/索引/保留策略（每日雷达是唯一稳定增长入口，齐平后使用时长上升会自己变贵）；⑤ dispatch 双 loadLedger + snapshot 内挂 operations 扫描（读路径刀主体）。

## 四、排期裁决（Owner）

- **#19/#20 可放行合并**（评审轴结论：P1 不破坏存量数据、不炸简报，属「应修可后续刀」）。
- **M27 收尾刀（Must 按序）**：F1-F5 修复头班车 → AI 提议起草场景（G 系列最后半块，依赖全就位）→ 资格收口（默认跑+徽章常驻，同代码面）→ 任务列表打磨+失败重跑 → 关系面板搜索 → G8 词条修订 + G9 守护测试顺手带走。**费用维度（G4）设计当刀定不下来则整块滑 M28**。
- **M28 读路径债刀**（独立，不并入 M27）：双 loadLedger 对齐 0051、syncTable 增量化、taskAudits 上限/轮转、FTS 触发器接线（前置：claims UPDATE 保 rowid）；滑落的费用维度也落此。
- **ADR 队列（不是代码刀）**：主键标记最小闭环语义（G1，或明示延期）；向量召回与多平台检索（G2/G3）排期 M29+ 或改判词条，二选一不让承诺悬空。
- **功能齐平验收定义**（六条，来自功能路线）：M27 交付场景/资格/硬顶/任务四词条全兑现 + 门禁全绿，ADR 队列出裁决，即可宣称功能面齐平；M28 起转巩固期。

## 五、Owner 门禁（基准树 09b6293，数字机械摘取自命令输出）

- typecheck / lint：通过（无输出）
- `npm run test`：**37 个文件 / 242 个测试**通过
- `npm run eval`：**4** 个阶段「通过」
- `npm run e2e`：**21 个 e2e** 通过（逐条清单在册复验；另记：一次高负载后台链跑报 20 passed、退出码 0 且无 failed/skipped 行——干净复跑 21/21 全清单，判为负载下计数异常，非产品问题，留观）
- `npm run build`：三段 built 通过
