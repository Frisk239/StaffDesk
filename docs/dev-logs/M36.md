# M36 — closeout face: README, release channel, and the fifth audit's head cars

## Scope

审计五轮排期首刀（收尾面）：G1 README / G2 Release 链路 / G3 LICENSE / G5 badge+dependabot + 顺刀修复（P2-1 排序键、E2 死噪音、导出失败反馈、P3-1 词条）+ 新诊断落地（CI brain.db 双候选、e2e 抗慢）。流程偏差记档：本刀 Owner 直接实现（审计五轮即勘察——M28 先例；文档主导 + 全部定位明确的小修）。

## Changes

- **G1 README**（根目录，中文）：一句话定位 / 核心概念（主张·账本·来源角色·简报·任务）/ 快速跑起来（Node 22 与 CI 一致、根目录聚合脚本、BYOK 说明）/ 架构一图（三层与分层纪律）/ 工程质量表 / ADR 精选 10 份（文件名逐一校验）/ 文档导览 / 边界红线。**G1 尾巴记档：GitHub description/topics 是仓库设置面，须用户在 GitHub 侧补**（一句话定位可直接复用 README 首行）。
- **G2 Release 链路**：package job 追加 tag 触发的 `gh release create`（windows runner 预装 gh CLI；job 级 `permissions: contents: write`——Actions 不支持 step 级，评审接受）；收尾动作：**用户推 `v0.2.0` tag 验证全链**（装机终验 → Release 发布 → README 链接可下载）。
- **G3 LICENSE**：MIT（2026, Frisk239）。
- **G5**：README CI badge + `.github/dependabot.yml`（npm 双目录 /app 与 /——根目录确有聚合 package.json；github-actions；weekly；minor+patch 分组、主版本单独过审）。
- **P2-1 排序键修复**：persist.ts `SELECT ... created_at AS createdAt`（as 不做运行时映射，此前 createdAt 恒 undefined 退化 id 序）；pruneOperations 加 limit 注入 seam（默认值不变，生产零变化）；**盲区测试**——真库构造 id 序与时间序倒挂的行（退化实现下必红，评审核验区分力有效）。
- **E2 死噪音**：ADD_SOURCE 标题回退的 fromUrl 死分支（case 顶部早退坐实不可达）、SET_VIEW selectedClaimId 自赋值——对照基线零行为变化。
- **导出失败反馈**：BriefView exportMarkdown 加 catch → TOAST「导出失败：detail」（M35 期间两度目击静默失败）；用户取消（返回 null）不弹——分流正确。
- **P3-1**：CONTEXT「简报」词条补「主键来源标注（0062）与整篇导出 Markdown（引用转脚注）」。
- **CI brain.db 双候选**：真实落点 `%APPDATA%\staffdesk-app\`（Electron 取 package name 与 productName 错位——#31 合并首跑诊断钉死；产品侧改名会迁移用户数据，暂不动记档）。
- **e2e 抗慢**：brief-export 的主键标注与 chip 落稳断言放宽 15s（鬼魅三连变挂点同根因=慢 runner 异步链超默认窗口）。

## Verification（数字机械摘取自命令输出）

- build / typecheck / lint / native:check：通过
- test：51 文件 / **360 测试**（P2-1 盲区 +1）；Lines 92.8%（门槛 ≥80%）
- eval：四阶段通过
- e2e：38 通过
- README 数字：51/360/92.8%/38/63 ADR 均机械可溯源（评审逐项核过；两轴整改后与本档一致）。

## Code review（/code-review 两路子代理，基点 main）

**Standards**：一处硬违规已修——README「M1–M36 验收记录」失真（实况：dev-logs 至 M34，M31/M35 为审计刀记录在审计报告）；其余数字/链接/命令/licence/dependabot 路径/gh CLI 预装全部核过。判断题已收：「来源分级」→「来源角色」（贴近 Avoid「来源等级」）、README 无内部机制名确认。
**Spec**：专项全过（盲区测试区分力、E2 零行为、取消不吞、双候选覆盖真实落点、Release 只 tag 触发）；README 数字自矛盾（359→360）与 Node 版本无佐证（→22 对齐 CI）已修；「description/topics 由用户侧补」本档记档兜住。

## Deviations / Notes

- Owner 直实现（审计即勘察，M28 先例）——文档主导刀，子代理实现无增益。
- M35 为审计刀无 MX.md（记录在 audit-2026-09-02-r5.md），README 文档导览已如实表述。
- **导出鬼魅根因终审（四连查后锁定，随本刀修复）**：App 的 `briefDraftingFor` 效应（App.tsx:57）会在首份生成期间再触发一次 `generateBrief`，而 IPC 守卫只跳过 START、**仍完整生成并追加第二份**（GENERATE_BRIEF_DONE 是 append）——渲染器是否观察到那个瞬态取决于广播时序，第二份简报与历史 chips 的存在是纯竞态。spec 种子改为显式生成两次（≥2 份在断言前成为事实），本地连跑五次 1.7-1.8s 全绿、CI 双运行绿。**顺带发现的产品级怪癖记 M37 队列：一次用户生成可能落两份简报（效应与调用方双生成）**——影响是历史条多一份，无正确性损害，修法待议（DONE 幂等或效应去重）。

## Debt carried / next

- **M37 体验与叙事刀**（审计五轮排期）：G4 截图与 ADR 主题索引、G6 渲染层护栏（store memo + 渲染计时）、G7 onboarding 后引导、E3 enqueueWrite 守卫合并、**双生成简报怪癖**（效应与调用方竞态，本档 Deviations 记档）。
- 观察：E4 retention 稳态、E5 stub 边界、E6 Settings 1489 行拆分候选。
- **收尾动作（用户侧）**：合并本 PR → 推 `v0.2.0` tag 验证 Release 全链 → GitHub 仓库 description/topics 补一句话定位。
- 下次审计刀 ~M40。
