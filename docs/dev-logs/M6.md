# M6 评测认证

状态：自测完成。

## 做了什么

- 内置三包虚构金标：面试（青浦书院）、求学（江澈）、选型（镜川）。
- 设置里插头走完三级：连通 → 能力探测 → 资格认证（跑当前工作区场景的金标包，出分数）。
- 编造率唯一红线 5%，超标警告不阻断。其余指标只展示。

## 怎么点

设置 → 模型 → 插头。1、2 级过了会出召回 / 忠实 / 未知遵守 / 编造率。编造率超过 5% 会标红，但仍能用。
