# 主张是去语境化的可核对命题，综合不进账本

主张若做成一段综合判断，冲突和过时会糊在一句话里，召回金标也无法按句对齐。切成用户可见的三元组又过碎，且强行消歧会抽出原文没说的话。StaffDesk 的主张是一条去语境化后仍可单独核对的命题（一个谓词；对象、时间在句内）。来源没断言的读法（例如「技术栈在过渡」）不是主张，只出现在简报或对象页的组织层，并必须指回主张。切不干净、指代解不开时宁可不抽。部分成立只用于单条命题的限定范围，不用于「一段话对了一半」。
